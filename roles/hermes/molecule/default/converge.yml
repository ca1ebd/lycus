---
- name: Converge
  hosts: all
  become: true

  vars:
    # The installer fetches a full Python/Node toolchain over the network. That
    # belongs in a dedicated scenario, not in the fast feedback loop — see the
    # note on hermes_install_agent in defaults/main.yml.
    hermes_install_agent: false

  roles:
    - hermes
