all:
  children:
    hermes_servers:
      hosts:
        # Replace with your VM's IP or hostname
        # hermes-vm:
        #   ansible_host: 192.168.1.100
        #   ansible_user: ubuntu
      vars:
        ansible_python_interpreter: /usr/bin/python3
